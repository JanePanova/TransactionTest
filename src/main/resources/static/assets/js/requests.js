// Generated by CoffeeScript 1.9.2
(function() {
  var requests;

  requests = angular.module('Requests', []);

  requests.factory('RequestFactory', function($http) {
    var accountUrl, factory, transactionUrl;
    accountUrl = '/api/account/';
    transactionUrl = '/api/transaction/';
    return factory = (function() {
      function factory() {}

      factory.getUsers = function() {
        return $http.get(accountUrl);
      };

      factory.getUsers = function(start) {
        if (start === void 0) {
          return $http.get(accountUrl);
        } else {
          return $http.get(accountUrl + '?start=' + start);
        }
      };

      factory.getUser = function(id) {
        return $http.get(accountUrl + id);
      };

      factory.createUser = function(name) {
        return $http.post(accountUrl, {
          name: name
        });
      };

      factory.deleteUser = function(id) {
        return $http["delete"](accountUrl + id);
      };

      factory.updateUser = function(user) {
        return $http.put(accountUrl + user.id, user);
      };

      factory.addMoney = function(id, money) {
        return $http.post(transactionUrl + 'add/' + id, {
          money: money
        });
      };

      factory.withdrawMoney = function(id, money) {
        return $http.post(transactionUrl + 'withdraw/' + id, {
          money: money
        });
      };

      factory.getTransactions = function() {
        return $http.get(transactionUrl);
      };

      factory.getTransactions = function(start) {
        if (start === void 0) {
          return $http.get(transactionUrl);
        } else {
          return $http.get(transactionUrl + '?start=' + start);
        }
      };

      factory.makeTransaction = function(from, to, money) {
        return $http.post(transactionUrl, {
          from: from,
          to: to,
          money: money
        });
      };

      return factory;

    })();
  });

}).call(this);

//# sourceMappingURL=requests.js.map
